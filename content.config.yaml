# ─── CMS Content Configuration ───────────────────────────
# Defines the structure of CMS-managed pages.
# Each page is stored in PageContent (payload JSON) with
# optional PageGroupItems for repeatable sections.

pages:
  home:
    label: Homepage
    fields:
      headline:
        type: text
        label: Headline
        default: "Welcome to Bauman Custom Products"
      subheadline:
        type: text
        label: Subheadline
        default: "Supplying dependable agricultural and stable tool brands to dealers across Canada."
      ctaText:
        type: text
        label: CTA Button Text
        default: "Get Started"
      ctaHref:
        type: text
        label: CTA Button Link
        default: "/auth/login"
      ctaSectionTitle:
        type: text
        label: CTA Section Title
        default: "Become a BCP Dealer"
      ctaSectionBody:
        type: text
        label: CTA Section Body
        default: "We are actively expanding our dealer network across Canada. If you're looking to add dependable farm and stable tool programs to your store, we'd welcome the opportunity to work together."
    groups:
      features:
        label: Features
        fields:
          icon:
            type: text
            label: Icon (emoji or text)
          title:
            type: text
            label: Title
          description:
            type: text
            label: Description

  about:
    label: About Page
    fields:
      title:
        type: text
        label: Page Title
        default: "About Us"
      body:
        type: richtext
        label: Body Content

  contact:
    label: Contact Page
    fields:
      title:
        type: text
        label: Page Title
        default: "Contact Us"
      subtitle:
        type: text
        label: Subtitle
        default: "Have questions? We'd love to hear from you."
      infoHeading:
        type: text
        label: Info Section Heading
        default: "Get in Touch"

  become-a-dealer:
    label: Become a Dealer
    fields:
      title:
        type: text
        label: Page Title
        default: "Become a BCP Dealer"
      description:
        type: text
        label: Page Description
        default: "Thank you for your interest in becoming a BCP wholesale partner. Please complete the form below and our team will contact you shortly."

  products:
    label: Products Page
    fields:
      title:
        type: text
        label: Page Title
        default: "Product Catalog"
      description:
        type: text
        label: Page Description
        default: "Browse our full range of farm, stable, and landscape tools."

collections:
  blog:
    label: Blog Posts
    fields:
      title:
        type: text
        label: Title
      excerpt:
        type: text
        label: Excerpt
      body:
        type: richtext
        label: Body
      date:
        type: text
        label: Date
      image:
        type: image
        label: Featured Image
